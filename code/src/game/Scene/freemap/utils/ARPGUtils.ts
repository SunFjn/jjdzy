class ARPGUtils {
	public constructor() {
	}

	/**
	 * 触点 x1,y1
	 * 目标坐标 x2,y2 height width
	 * 
	 * */
	static hitTestPoint(x1, y1, x2, y2, height, wid = 100) {
		let ret = false;
		return ret;
	}
}