package com.teamtop.system.hero;


public class TestHero {
	public static void main(String[] args) {
		/*GameCache gameCache = new GameCache();
		ConcurrentHashMap<Long, Hero> heroMap = gameCache.attr(GameCache.heroMap).get();
		ArrayList<Channel> channelList = gameCache.attr(GameCache.channelList).get();*/
		
		/*HeroCache heroCache = HeroCache.getIns();
		heroCache.initCache();
		Hero h = new Hero();
		long hid = 3;
		heroCache.putConcurrentHashMapValue(HeroCache.HERO_MAP, hid, h);
		Hero hero = heroCache.get(HeroCache.HERO_MAP,Hero.class,hid);
		System.err.println(hero);
		*/
		
	}
}
